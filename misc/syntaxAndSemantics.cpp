#include <iostream>

using namespace std;

int main() {
    // Syntax: Rules for writing code
    int x = 5;
    int y = 3;
    int z = x + y;

    // Semantics: Meaning of code
    cout << "The sum of " << x << " and " << y << " is " << z << endl;

    return 0;
}
